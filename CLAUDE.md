# OpenAgent-Desktop

Local-first agent orchestration studio MVP.

## Commands
- `npm test` — run Node built-in tests
- `npm start` — launch the local API + browser UI on port 4488

## Conventions
- Use CommonJS + Node built-ins only for the MVP slice
- Keep the UI zero-build (single server-rendered HTML page)
- Add/adjust tests before changing orchestration snapshot logic
